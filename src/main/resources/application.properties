# ===================================================================
# COMMON SPRING BOOT PROPERTIES
#
# This sample file is provided as a guideline.
# Do NOT copy it in its entirety to your own application.
# ===================================================================

server.port = 8080
spring.mustache.suffix = .html
spring.mvc.static-path-pattern = /static/**

spring.datasource.url = jdbc:h2:~/partner.slipp;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName = org.h2.Driver
spring.datasource.username = sa
spring.datasource.password =
spring.jpa.database-platform = org.hibernate.dialect.H2Dialect

# sql statements display on console window
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true

spring.jpa.hibernate.ddl-auto = create-drop
spring.datasource.data = classpath:data-h2.sql

spring.mustache.expose-session-attributes = true

#spring.jpa.generate-ddl = true
#spring.datasource.initialization-mode = never
